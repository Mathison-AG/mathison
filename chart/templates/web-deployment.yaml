apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "mathison.fullname" . }}-web
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "mathison.labels" . | nindent 4 }}
    app.kubernetes.io/component: web
spec:
  replicas: {{ .Values.web.replicas }}
  selector:
    matchLabels:
      {{- include "mathison.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: web
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        {{- include "mathison.labels" . | nindent 8 }}
        app.kubernetes.io/component: web
    spec:
      serviceAccountName: {{ include "mathison.serviceAccountName" . }}
      terminationGracePeriodSeconds: 30
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
        - name: web
          image: {{ include "mathison.image" (dict "image" .Values.image.web "global" .Values.global "chart" .Chart) }}
          imagePullPolicy: {{ .Values.image.web.pullPolicy }}
          ports:
            - name: http
              containerPort: 3000
              protocol: TCP
          env:
            - name: NODE_ENV
              valueFrom:
                configMapKeyRef:
                  name: {{ include "mathison.configMapName" . }}
                  key: NODE_ENV
            - name: MATHISON_MODE
              valueFrom:
                configMapKeyRef:
                  name: {{ include "mathison.configMapName" . }}
                  key: MATHISON_MODE
            - name: LLM_PROVIDER
              valueFrom:
                configMapKeyRef:
                  name: {{ include "mathison.configMapName" . }}
                  key: LLM_PROVIDER
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: {{ include "mathison.secretName" . }}
                  key: DATABASE_URL
            - name: REDIS_URL
              valueFrom:
                configMapKeyRef:
                  name: {{ include "mathison.configMapName" . }}
                  key: REDIS_URL
            - name: AUTH_SECRET
              valueFrom:
                secretKeyRef:
                  name: {{ include "mathison.secretName" . }}
                  key: AUTH_SECRET
            - name: AUTH_URL
              valueFrom:
                configMapKeyRef:
                  name: {{ include "mathison.configMapName" . }}
                  key: AUTH_URL
                  optional: true
            - name: MATHISON_BASE_DOMAIN
              valueFrom:
                configMapKeyRef:
                  name: {{ include "mathison.configMapName" . }}
                  key: MATHISON_BASE_DOMAIN
                  optional: true
            - name: ANTHROPIC_API_KEY
              valueFrom:
                secretKeyRef:
                  name: {{ include "mathison.secretName" . }}
                  key: ANTHROPIC_API_KEY
                  optional: true
            {{- if .Values.config.tenantIngress.enabled }}
            - name: INGRESS_ENABLED
              valueFrom:
                configMapKeyRef:
                  name: {{ include "mathison.configMapName" . }}
                  key: INGRESS_ENABLED
                  optional: true
            - name: INGRESS_CLASS
              valueFrom:
                configMapKeyRef:
                  name: {{ include "mathison.configMapName" . }}
                  key: INGRESS_CLASS
                  optional: true
            - name: TLS_ENABLED
              valueFrom:
                configMapKeyRef:
                  name: {{ include "mathison.configMapName" . }}
                  key: TLS_ENABLED
                  optional: true
            - name: TLS_CLUSTER_ISSUER
              valueFrom:
                configMapKeyRef:
                  name: {{ include "mathison.configMapName" . }}
                  key: TLS_CLUSTER_ISSUER
                  optional: true
            {{- end }}
            {{- with .Values.web.extraEnv }}
            {{- toYaml . | nindent 12 }}
            {{- end }}
          startupProbe:
            httpGet:
              path: /api/health
              port: http
            initialDelaySeconds: 5
            periodSeconds: 5
            failureThreshold: 12
          livenessProbe:
            httpGet:
              path: /api/health
              port: http
            periodSeconds: 15
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /api/health?ready=true
              port: http
            periodSeconds: 10
            failureThreshold: 2
          {{- with .Values.web.resources }}
          resources:
            {{- toYaml . | nindent 12 }}
          {{- end }}
      {{- with .Values.web.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.web.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.web.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
