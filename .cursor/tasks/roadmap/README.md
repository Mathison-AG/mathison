# Roadmap

Lightweight idea parking lot. Each file is a short idea sketch — not a full task spec.

**Format**: One `.md` file per idea. Keep it to a title + a few bullet points.

**Lifecycle**: When an idea is ready to build, refine it into a proper task in `backlog/` and delete it from here.
